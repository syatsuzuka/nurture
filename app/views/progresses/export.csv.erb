<%- headers = ['TargetName', 'TargetScore', 'Date', 'Score', 'Comment', 'CreatedAt', 'UpdatedAt'] -%>
<%= CSV.generate_line headers %>
<%- @progresses.each do |progress| -%>
<%= CSV.generate_line([progress.target.name, progress.target.score, progress.date, progress.score, progress.comment, progress.created_at, progress.updated_at]) -%>
<%- end -%>
