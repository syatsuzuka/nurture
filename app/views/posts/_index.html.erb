<h1>Posts#index</h1>
<p>Find me in app/views/posts/index.html.erb</p>

  <% @posts.each do |p| %>
    <h2> <%= p.title %> </h2>
    <p><%= p.content %></p>
    <%= link_to "View Post", "posts/#{p.id}" %>
  <% end %>
